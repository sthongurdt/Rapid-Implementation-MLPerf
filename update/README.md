# What's going on here?

Here you'll find two directories with the following contents:
- The **device** directory contains the Dockerfiles for the different devices on which tests were performed.
- The **tools** directory contains a .sh file for configuring a dataset with almost 90% accuracy. If you want a better one, you should consult the MLPerf documentation to create your own dataset.
